# Proyecto para prueba de diagnostico

Para hacerlo funcionar, crear archivo .env en la carpeta raiz, con los siguientes valores:

```DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASS=
```

Luego es ejecutar:

` php -S localhost:8000`
